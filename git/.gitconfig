# Git config file

# /etc/gitconfig
# ~/.gitconfig 
# /path/to/project/.git/config

[user]
	email = $EMAIL
	name = !bash -c '"echo \"$USER\";"'
[core]
	editor = vi
[color]
	ui = true
[alias]
	st = status
	lgg = log --graph --oneline --decorate --abbrev-commit --all
	ci = commit
	modU = !git submodule deinit -f --all && git submodule init && git submodule update --remote
	modS = !git submodule deinit -f $1 && git submodule init $1 && git submodule update --remote $1 && :

#	modU  # update all submodules to latest remote version
#	modS  # update arg '$1' to latest remote version

#	test = !bash -c '"echo \"User =  $USER\";"'
